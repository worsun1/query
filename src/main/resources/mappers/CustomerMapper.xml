<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.query.mapper.CustomerMapper">

    <resultMap id="CustomerResultMap" type="com.example.query.domain.Customer">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="email" property="email" />
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="BaseColumnList">
        id, name, email, created_at
    </sql>

    <select id="findById" parameterType="long" resultMap="CustomerResultMap">
        SELECT
        <include refid="BaseColumnList" />
        FROM customers
        WHERE id = #{id}
    </select>

    <select id="searchCustomers" resultMap="CustomerResultMap">
        SELECT
        <include refid="BaseColumnList" />
        FROM customers
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
